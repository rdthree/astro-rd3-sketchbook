---
// src/components/Navigation.astro
const links = [
  { href: '/', text: 'Home' },
  { href: '/about', text: 'About' },
];
---

<nav>
  <ul>
    {links.map(link => (
      <li><a href={link.href}>{link.text}</a></li>
    ))}
  </ul>
</nav>
