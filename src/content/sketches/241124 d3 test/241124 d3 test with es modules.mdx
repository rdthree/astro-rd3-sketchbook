import D3Sketch from '@D3Sketch';
import CodeSnippet from '@CodeSnippet';
import Sketch from '@Sketch';

## D3 sketch loaded via ES modules.

Loading D3 sketches via ES modules, and a custom astro component.
```jsx
<D3Sketch sketchId="d3test_241124" />
```
Also works Loading D3 sketches via ES modules, and a combined astro component.

```jsx
<Sketch sketchId="d3test_241123" renderer="d3" />
```

<Sketch sketchId="d3test_241124" renderer="d3" />