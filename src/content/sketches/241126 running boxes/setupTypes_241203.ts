export type Parameters = {
    boxRotationSpeed: number;
    lightRotationSpeed: number;
    boxColor: string;
    lightColor: string;
    boxScale: number;
};

export type Cursor = {
    x: number;
    y: number;
};
