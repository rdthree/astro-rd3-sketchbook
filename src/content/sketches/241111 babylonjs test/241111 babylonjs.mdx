
import BabylonSketch from '@BabylonSketch';

# Babylon.js Test

This sketch uses an astro component instead of the jsx component that was initially used. It consists of a workaround where instead of depending on frontmatter, it uses a 'renderer' typescript file and a 'loader' astro component.

<BabylonSketch sketchId="babylon-test-simple" />

The sketch should load above this line of text.
